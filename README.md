# chatbot-rb
I've built a simple rule-based chatbot that responds to specific inputs using predefined rules. The system is easily extendable — new rules can be added directly within the code to handle more user inputs. The chatbot includes a FastAPI backend and a web-based frontend interface.
